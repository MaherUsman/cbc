(function($) {
    "use strict"

    //date picker classic default
    $('.datepicker-default').pickadate({
        min: new Date(),      // Disable past dates by setting the minimum date to today
    });

})(jQuery);
